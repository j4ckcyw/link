{
  "name": "link",
  "version": "1.0.0",
  "description": "Open all links under selected area in new window",
  "scripts": {
    "start": "beefy src/link.js 8080 --live",
    "clean": "rm browserified.js",
    "build": "browserify src/link.js -o browserified.js",
    "test": "echo after npm start, open test.html in your browser"
  },
  "license": "MIT",
  "devDependencies": {
    "rxjs": "^5.0.0-beta.8"
  }
}
